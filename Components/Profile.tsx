import { ScrollView, StyleSheet, Text, View,} from 'react-native';

const Profile = () => {
    return (
        <ScrollView>
            <View>
                
            </View>
        </ScrollView>
    )
}
export default Profile;